import React from "react";
import CardLayout from "../components/CardLayout";
import { Stack } from "@mui/material";

const Home = () => {
  return (
    <Stack>
      <CardLayout />
    </Stack>
  );
};

export default Home;
